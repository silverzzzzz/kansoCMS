{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "types": ["node"],
    // This package runs directly in Node, without a bundler.
    "module": "NodeNext",
    "moduleResolution": "NodeNext"
  },
  "include": ["src", "bin"]
}
